@{
    ViewData["Title"] = "¡Hora de escapar!";
}

<style>
    body {
        background-color: #1a1a1a;
        color: #f8f8f8;
        font-family: 'Courier New', Courier, monospace;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100vh;
        text-align: center;
    }

    h1 {
        color: red;
        font-size: 3rem;
        margin-bottom: 1rem;
        animation: blink 1s step-start infinite;
    }

    @@keyframes blink {
        50% {
            opacity: 0;
        }
    }

    #contador {
        font-size: 4rem;
        color: red;
        margin: 1rem 0;
    }

    p {
        max-width: 600px;
        margin-bottom: 2rem;
    }

    .btn {
        padding: 12px 24px;
        background-color: #2a9d8f;
        color: white;
        text-decoration: none;
        font-weight: bold;
        border-radius: 8px;
        font-size: 1.1rem;
    }

    .btn:hover {
        background-color: #21867a;
    }
</style>

<h1>¡ES TU MOMENTO!</h1>
<div id="contador">45:00</div>
<p>Un caos se desata en el comedor. Las pandillas se enfrentan y los guardias se ven forzados a intervenir. ¡Es tu única oportunidad de escapar mientras nadie te vigila!</p>

<a class="btn" href="/Home/Sala1">Comenzar el Escape</a>

<script>
    let minutos = 44;
    let segundos = 59;

    const contador = document.getElementById("contador");

    const intervalo = setInterval(() => {
        if (segundos === 0) {
            if (minutos === 0) {
                clearInterval(intervalo);
                contador.textContent = "¡Tiempo terminado!";
                return;
            }
            minutos--;
            segundos = 59;
        } else {
            segundos--;
        }

        let m = minutos.toString().padStart(2, '0');
        let s = segundos.toString().padStart(2, '0');
        contador.textContent = `${m}:${s}`;
    }, 1000);
</script>
